name: CI
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: [macos-11, macos-10.15]
    steps:
      - uses: actions/checkout@v2

      - name: Setup dotfiles
        run: IN_CI=true sudo ./setup.sh
